package org.age.hz.core.services.worker.enums;

public enum WorkerState {

    OFFLINE,
    RUNNING,
    CONFIGURED,
    EXECUTING,
    PAUSED,
    FINISHED,
    COMPUTATION_CANCELED,
    COMPUTATION_FAILED,
    FAILED,
    TERMINATED

}
